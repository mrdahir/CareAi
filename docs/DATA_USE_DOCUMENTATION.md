# Data use Documentation

**HASH Innovation Challenge — Prototype Submission**  
**Deliverable 4 of 7:** Data use Documentation  
**Project:** CareApp  
**Source Code Repository:** https://github.com/mrdahir/CareAi/

---

## A. Data Sources

### Challenge datasets used from the DASSA platform

| Dataset | Description | Location in repo / pipeline |
|---------|-------------|----------------------------|
| *Reversing the Stall in Fertility Decline in Western Kenya — Programme Monitoring Data* | Client service statistics and community mobilisation registers | External folder (mounted in Docker): `reversing-the-stall-in-fertility-decline-in-western-kenya-programme-monitoring-data-All-2026-05-19_1703/` |
| `Client_Service_Statistics.csv` | FP client visits, methods adopted, demographics, counselling flags | Processed by `backend/scripts/data_processing/western_kenya_pipeline.py` |
| `Mobilisation_Activity_Register.csv` | Community activities, reach estimates | Same pipeline |

**Processed output:** `backend/data/processed/western_kenya_stats.json` (aggregate statistics only; no individual records exposed via API).

**Environment override:** `WESTERN_KENYA_DATA_DIR` (see `docker-compose.yml`).

### Public datasets integrated and any externally sourced datasets

| Source | Use in CareApp | Authority tag |
|--------|----------------|---------------|
| WHO (2022) contraceptive methods guidance | Knowledge base method profiles, myths, FAQs | `WHO_2022_contraceptive_methods` |
| CDC contraception guidelines | Knowledge base citations | `CDC_guidelines` |

Structured content files:

- `backend/data/knowledge_base/contraceptive_methods.json` (6 methods)
- `backend/data/knowledge_base/common_myths.json` (22 myths)
- `backend/data/knowledge_base/faqs.json` (51 FAQs)

### Externally sourced datasets

| Dataset | Source | Use |
|---------|--------|-----|
| APHRC-style sample CSV | Generated by `backend/scripts/data_processing/run_pipeline.py` if no raw file present | Demo `contraceptive_stats.json`; not served by production API |
| Google Gemini / Groq APIs | External inference (optional) | Chat, myth/FAQ fallback, KB translation (`translate_kb.py`) |

---

## B. Data Management

### Data cleaning procedures

**Western Kenya pipeline (`western_kenya_pipeline.py`):**

- Method name normalisation via `METHOD_MAP` (e.g., pills→pill, iucd→iud)
- Counselling rate: proportion of rows where `counseled == "yes"`
- County and method aggregation with top-N truncation (top 10 methods, top 15 counties)
- Mobilisation reach: sum of `maleadult`, `maleyouth`, `femaleadult`, `femaleyouth` columns

**Knowledge base generation (`gen_kb.py`):**

- Template-based myth and FAQ generation with consistent schema (id, i18n fields, sources, severity)

### Feature engineering methods

| Feature | Engineering method | Consumer |
|---------|-------------------|----------|
| `regional_method_share` | Percentage of visits by normalised method | Recommendation engine regional overlay |
| `counselled_rate` | Aggregated yes/no ratio | Programme API, chat context |
| County-level method mix | Group-by county + method counts | `GET /api/programme/stats?county=` |
| KB context snippets | Lexical match + truncation (≤300 chars) | LLM RAG injection |

### Data quality checks

| Check | Implementation |
|-------|----------------|
| Missing Western Kenya CSV | Pipeline logs warning; programme API returns 404 |
| Integration test skip | `test_programme_api.py` skips if stats file absent |
| KB load validation | `test_knowledge_base.py` — load, myth match, FAQ search |
| Empty LLM response | Gemini/Groq retry up to 3 times; then KB fallback |

---

## C. Data Governance

### Data permissions and licenses for public and external datasets used

| Data asset | Permission / licence | Use |
|------------|---------------------|-----|
| DASSA Western Kenya programme data | Challenge dataset terms (HASH / DASSA platform) | Challenge submission; aggregate processing only |
| WHO / CDC referenced content | Public health guidance (citation required) | Educational use |
| MIT-licensed codebase | MIT Licence | Open-source attribution |
| Gemini / Groq | Provider terms of service | API inference via environment configuration |

### Privacy protection measures

- Programme API exposes **aggregate statistics only** — no individual client records
- Chat persistence uses **anonymous UUIDs**; no mandatory PII collection
- User guide advises against sharing national IDs in chat
- Rate limiting reduces bulk data extraction (100 req/60 s default)

### Compliance with ethical standards

- Medical disclaimer on all chat responses and frontend footer
- Non-diagnostic scope enforced in LLM system prompts
- Health content is aligned with WHO/CDC sources and non-diagnostic scope enforced in LLM system prompts
- Ethical analysis is documented in the Technical Report, Section D (Ethical and Responsible AI Considerations)

---

*End of document.*
