# CareApp backend — use Python 3.11+ (3.11–3.13 recommended; Docker uses 3.11)

# FastAPI & Web
fastapi>=0.109.0,<0.116
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.23
alembic>=1.13.1
asyncpg>=0.29.0
aiosqlite>=0.19.0

# AI/LLM (Gemini + Groq via httpx; anthropic optional if reverting Claude)
# anthropic>=0.25.0

# Validation & config
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Utilities
python-dotenv>=1.0.0
python-slugify>=8.0.1
httpx>=0.27.0

# Logging
structlog>=24.1.0

# Testing & dev
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0

# Deployment
gunicorn>=21.2.0

# Optional PostgreSQL (pip install -r requirements-postgres.txt)
# Optional pandas analytics (pip install pandas) — pipeline uses stdlib csv
